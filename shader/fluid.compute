#version 460 core

layout( local_size_x = 1 ) in;


struct Particle
{
    vec3 Position;
    vec3 Velocity;
    vec3 force;
    vec3 surfNormal;
    float density;
    float pressure;
    float padding;
};

layout(std430, binding = 1) buffer ParticleBuffer
{
    Particle data[];
};


void main()
{

}